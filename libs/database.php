<?php

  // Open database connection using PEAR::DB
  /*
  $dsn  = $vsm_dbtype . '://' . $vsm_dbuser . ':' . $vsm_dbpass . '@';
  $dsn .= $vsm_dbserver . '/' . $vsm_dbname;
  $maildb = DB::connect($dsn);
  if (DB::isError($maildb)) {
    die($maildb->getMessage());
  }
  $maildb->setFetchMode(DB_FETCHMODE_ASSOC);
  */

?>
